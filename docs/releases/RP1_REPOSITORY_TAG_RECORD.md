# RP1 repository tag record

Proposed tag: `finora-rp1-final-inconclusive-v1.0.0`

Current commit: `bf80566ddaebd3c4926885d18e3ea32205286ee1` on `main`.
The tag has not been created or pushed. The worktree is dirty, so a clean archival
commit is required first. After approval: `git tag -a finora-rp1-final-inconclusive-v1.0.0 -m "RP1 final inconclusive archive" <archive-commit>`; verify with `git tag --list`; then push with `git push origin finora-rp1-final-inconclusive-v1.0.0`.
